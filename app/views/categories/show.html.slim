h1
  | Recent Projects
.projects-container
  - @category.projects.recent.each do |r|
    = render 'project', project: r
h1
  | Other Projects
.projects-container
  - @category.projects.old.each do |o|
    = render 'project', project: o

